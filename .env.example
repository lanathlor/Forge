# Path to the directory containing your git repositories
WORKSPACE_ROOT="/path/to/your/projects"

# Database
# SQLite (default, good for local use):
DATABASE_URL="./forge.db"
# PostgreSQL (for production):
# DATABASE_URL="postgresql://user:password@localhost:5432/forge"

# AI provider: claude-sdk | codex-sdk | claude-code | fake
AI_PROVIDER="claude-sdk"

# Anthropic API key (used when AI_PROVIDER=claude-sdk)
ANTHROPIC_API_KEY="sk-ant-..."

# Claude Code CLI command (used when AI_PROVIDER=claude-code)
# CLAUDE_CODE_PATH="claude"

# OpenAI API key (used when AI_PROVIDER=codex-sdk)
# OPENAI_API_KEY="sk-..."

# Server
NODE_ENV="development"
PORT="3000"

# Docker: path to your Claude config directory (mounted into the container)
# CLAUDE_CONFIG_DIR="~/.claude"

# Docker: host UID/GID for file permission matching in the dev container
# USER_UID=1000
# USER_GID=1000

# Docker (production with --profile postgres):
# POSTGRES_DB=forge
# POSTGRES_USER=forge
# POSTGRES_PASSWORD=changeme
